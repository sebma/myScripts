Get-NetTCPConnection -State listen | select local* , remote* , state , OwningProcess , @{ Name="ServiceName" ; Expression={ (Get-WmiObject win32_service | ? ProcessId -eq $_.OwningProcess).Name } } | sort-object LocalPort
